#include <iostream>

using namespace std;

int main()
{
   int n,no,i;
   cin>>n>>no;
   int a[50];
   for(int i=0;i<n;i++)
   
   {
       cin>>a[i];
   }
   int count=0;
   for(int i=0;i<n;i++)
   {
     
           if(a[i]==no)
       {
           count++;
       }
   }
   cout<<count;
}
