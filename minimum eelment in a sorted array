#include<iostream>
using namespace std;

int main()
{
    int n,i;
    cin>>n;
    int a[50];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    int min=a[0];
   for(int i=0;i<n;i++)
   {
       if(min>a[i])
       {
           min=a[i];
       }
   }
   cout<<min;
}
